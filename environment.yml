name: xeus-python-kernel
channels:
  - https://repo.mamba.pm/emscripten-forge
  - conda-forge
dependencies:
  - pip
  - xeus-python
  - sqlalchemy
  - pip:
    - jupyter_anywidget_pglite
    - anywidget==0.9.13
